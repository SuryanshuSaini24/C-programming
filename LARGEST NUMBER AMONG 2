#include<stdio.h>
int main() {
  int num1,num2;
  printf("ENTER NUM 1:");
  scanf("%d",&num1);
  printf("ENTER NUM 2:");
  scanf("%d",&num2);
  (num1>num2)?printf("%d is largest",num1): printf("%d is largest",num2);

  return 0;
}
