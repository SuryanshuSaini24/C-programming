#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int N,Sum;
    scanf("%d",&N);
    while(N!=0){
        Sum+=N%10;
            N=N/10;
        
    }
    printf("%d",Sum);
    
    return 0;
}
